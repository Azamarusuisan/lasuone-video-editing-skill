# GitHub保存版 — 2026-09-14

保存先: `Azamarusuisan/lasuone-video-editing-skill`（公開）。学習途中のコード・文書・解析記録・未インストールの動画編集スキル草案を保存する。

研究コピーの動画・音声・静止画像、仮想環境、コンパイル済み`analyze-frames`はGitに含めない。画像のファイル名・参照PTS・OCR・未校正ASR等のJSONは含む。参照先のメディア自体はローカルにのみ存在し、リポジトリの取得で同梱されたことにはならない。研究資料の制作再利用権はUNKNOWN。

今回の読み取り照合時点（2026-09-14 20:34 JST）では研究コピー188本、映像の全尺差分候補解析188本、概要capture・OCR各188本、音量計測188本、未校正ASR候補あり188本。取得済み全動画の機械解析バッチは完走し、該当プロセスは終了した。古いPIDや処理状態を引き継いで二重起動しない。

`evidence/COVERAGE.json`と草案の凍結集計は2026-09-14T11:31:34.991Z時点で整合している。COVERAGEへ集計済みの全尺分散reviewは28本、部分静止観察は56本。R9/rUZ/Z53/N2の補助進捗は別スキーマのまま集計へ加えていない。

R9は1101画像、rUZは799画像、Z53は1059画像、N2は713画像の全概要グリッドを確認済み。N2の最終公称グリッドは10680秒で、動画尺に対する最後の約19.73秒は画像なし。全編の連続映像・音声確認完了は0本、`production_ready=false`、CHANNEL_MATCHはNOT_EVALUATED。

既存チェックに加え、`node check-evidence.mjs`で次を照合する。

- 草案13ファイルとルート文書のバイト一致・SHA-256。
- R9/rUZ/Z53/N2の進捗、画像参照、草案snapshotとの一致。
- 局所21区間551画像、59イベントのPTS、2イベントの時刻窓とcaptureの一致。
- 終端画像なしのレコードを画像数へ含めないこと、現存する解析JSONの構造。

`check-evidence.mjs`と通常の再集計にはローカル研究画像が必要。メディアを含まないcloneでこれらをそのまま完走できるとはしない。各`*.mjs check`と草案の構造validatorは別のチェックであり、ASR正答率・音の聴取・制作可能性を認定しない。実処理スクリプトのFFmpeg/Whisper/Node等のパスは現Macの値。
